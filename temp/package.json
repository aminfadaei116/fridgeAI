{
  "name": "savor-fridge-demo",
  "private": true,
  "version": "2.0.0",
  "description": "Static viewer for backend/pipeline.sh experiment output, served as Cloudflare Workers assets.",
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "predev": "node scripts/sync.mjs",
    "dev": "npx wrangler dev",
    "predeploy": "node scripts/sync.mjs",
    "deploy": "npx wrangler deploy"
  }
}
