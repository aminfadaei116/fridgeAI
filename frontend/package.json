{
  "name": "savor-fridge-demo",
  "private": true,
  "version": "1.0.0",
  "description": "A self-contained fridge inventory demo for Cloudflare Workers.",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
