# API keys

`Gemini_API.txt` — one line, the Gemini API key only (no labels, no extra text).
This directory is git-ignored (except this README). Never commit keys.

Override without touching the file: `export GEMINI_API_KEY=...`
